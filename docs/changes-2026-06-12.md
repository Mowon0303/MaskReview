# Change summary — 2026-06-12

Suggested commit message (the repo is not yet a git repo; `git init` first if you want to commit):

```
feat: confidence-aware review queue, ground-truth J&F eval, multi-point + click corrections, GPU

Evaluation (P7 — ground-truth quality scoring)
- Add src/quality_metrics.py: region IoU (J), boundary F, J&F sequence scoring, indexed mask IO.
- evaluation.py: optional per-case `ground_truth_mask_dir`; score J/F/J&F before & after corrections;
  new gt_jf_before/after/delta (+J/F) CSV columns, a Ground-Truth Quality report section, and a
  Mean J&F column in the threshold-calibration report. manifest_template emits a `ground_truth_mask_dir`
  placeholder.

Review queue ← SAM2 confidence (#1)
- sam2_runner.py: expose per-frame confidence (sigmoid of peak object logit) via `last_confidences`.
- pipeline.py: record `mask_confidences`; add `low_mask_confidence` signal; annotate every queued
  frame with `confidence` for triage (v1). Collapse consecutive empty+low-confidence frames into one
  onset interaction (`status: low_confidence_empty`, `ReviewPolicy.collapse_low_confidence_runs`) so a
  disappearance no longer inflates the interaction estimate (v2). Add a unified, rankable
  `review_score` per item (`REVIEW_REASON_WEIGHTS` + confidence/collapse state); the UI reads it (v3).
- ReviewPolicy: +low_confidence_threshold, +collapse_low_confidence_runs.

Corrections ← real action counts (#2)
- corrections.py / evaluation.py: point corrections may carry several clicks (`x,y;x,y`, or lists in
  manifests); `estimated_interactions` = number of clicks (box = 1), so `actual_interactions` reflects
  true human effort instead of one-per-frame. sam2_runner applies ALL points with per-point labels;
  validate_corrections checks every point.

Correction UX ← click-to-correct (#3)
- app.py: clicking the source frame stages points (multi-click) / a two-click tight box via
  gr.Image.select -> on_frame_click; coordinate fields stay editable. Pure helpers unit-tested;
  CONF- reason styled. (Live Gradio wiring needs an interactive app run to confirm.)

GPU
- Verified device-agnostic path on an RTX 3060 (torch 2.5.1+cu124): ~0.35-0.5 s/frame (~12-16x CPU),
  bfloat16 autocast. No separate "GPU build" — `--device cuda` / `MR_DEVICE=cuda`.

Tests: 22 -> 48, all passing (python -m unittest discover -s tests).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

## Files

**New**
- `src/quality_metrics.py` — J / boundary-F / J&F + indexed-mask IO.
- `tests/test_quality_metrics.py`.
- `docs/changes-2026-06-12.md` (this file).

**Modified — source**
- `src/pipeline.py` — ReviewPolicy (+2 fields), `build_review_queue` (+confidence signal, triage
  annotation, absence-run collapse, `review_score`), `REVIEW_REASON_WEIGHTS`, `compute_review_score`,
  `_consecutive_runs`, `validate_corrections` (all points), `mask_confidences` in metrics, confidence
  plumbed through `run`/`run_with_corrections`.
- `src/sam2_runner.py` — per-frame confidence (`last_confidences`), `_select_object_mask` returns
  (mask, confidence), `_add_points_prompt` applies all points with per-point labels.
- `src/corrections.py` — multi-point `build_correction`, `parse_points_xy`, interaction = click count.
- `src/evaluation.py` — `ground_truth_mask_dir` + J&F scoring + report/CSV columns; multi-point
  manifest corrections (`parse_manifest_points`).
- `src/manifest_template.py` — `ground_truth_mask_dir` placeholder + instruction.
- `src/app.py` — `apply_point_click` / `apply_box_click` / `on_frame_click`, `review_frame_image.select`
  wiring, UI hints, `CONF-` reason style, `infer_item_risk` reads `review_score`, multi-point hint.

**Modified — tests** (48 total, all green)
- `tests/test_pipeline.py`, `tests/test_corrections.py`, `tests/test_evaluation.py`, `tests/test_app.py`.

**Modified — docs**
- `README.md` (实验结果 section + signals/fields + click-to-correct), `CHANGELOG.md` (Unreleased),
  `PLAN_TRACKER.md` (P7 实验记录, item 5/7/8), `docs/landing_readiness.md` (gaps #2, #3).

## Not committable (data / .gitignore'd)
- Experiment artifacts: `outputs/davis-*`, `outputs/real-bedroom-full`, `outputs/real-smoke-80`,
  `data/davis/*`, `data/mose/`, and everything under `D:/MOSE_data/` (26 GB MOSE zip, extracted videos,
  cu124 pip cache). `.gitignore` already excludes `outputs/*`, `data/eval_videos/*`, etc.
- Two kept evidence images: `outputs/_davis_drift_pred_vs_gt.jpg`, `D:/MOSE_data/_398d41f5_before_after.jpg`.

## Verification highlights (real data)
- Ground-truth J&F: 5 DAVIS single-object seqs mean ~0.96 (queue near-silent); 15 MOSE crowded seqs
  mean ~0.88, two natural drift+recovery samples — 23b9a3ea 0.79->0.92, 398d41f5 0.59->0.68.
- Confidence is complementary to geometry: 6da17988 (object absent) confidence ~0.02 vs 1.00 present
  (50/50 absent frames); 398d41f5 (wrong-object drift) confidence stays 0.985 (geometry catches it).
- Absence-run collapse: 6da17988 interaction estimate 54 -> 5, queue still 54 visible.
- review_score: 398d41f5 worst drift frame (IoU 0.0) ranks #1; 49 collapsed-absence frames rank 0.15.
- Multi-point: a 2-click correction propagates through real SAM2 on GPU, actual_interactions = 2.
